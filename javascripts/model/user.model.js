/*global define */

/**
 * A simplified UserModel
 *
 * @author Stephen Kao
 */
define([
	'backbone'
], function (
	Backbone
) {
	'use strict';

	var UserModel = Backbone.Model.extend({
	});

	return UserModel;
});
