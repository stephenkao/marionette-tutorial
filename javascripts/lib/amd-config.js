/*global requirejs */

(function () {
	'use strict';

	requirejs.config({
		paths: {
			jquery: 'lib/jquery',
			underscore: 'lib/underscore',
			backbone: 'lib/backbone',
			'backbone.marionette': 'lib/backbone.marionette'
		}
	});
});
