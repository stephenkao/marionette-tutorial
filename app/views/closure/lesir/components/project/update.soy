{namespace lesir.components.project}

/**
 * @param date
 * @param title
 * @param user
 */
{template .updateListItem}
	<span class="delta">{$date}</span>
	<span class="beta">{$title}</span>
	<span class="epsilon">by</span>
	<span class="gamma">{$user}</span>
{/template}
